PORT=4000
DATABASE_URL="file:./dev.db"
CORS_ORIGIN="http://localhost:5173"
# At least 32 characters; change before exposing the app to a network.
JWT_SECRET="wow_metrics_local_dev_change_before_production_use"
